[tool:pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts =
    --strict-markers
    --verbose
markers =
    slow: marks tests as slow (allocate real devices, cost minutes)
    integration: marks tests as integration tests
    live: marks tests that hit live Sauce Labs APIs (require credentials)
