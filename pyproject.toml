[project]
name = "sauce-api-mcp"
version = "0.1.0"
description = "MCP Server that models the Sauce Labs API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[server]>=1.9.3",
]
